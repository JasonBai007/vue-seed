import * as types from '../mutation-types.js'
import axios from 'axios' //如果有action

const module1 = {
    state: {

    },
    mutations: {

    },
    actions: {
        // search({ commit }) {
        //     axios({

        //     }).then(res => {
        //         console.log(res)
        //     }).catch(error => {
        //         console.log(error)
        //     })
        // }
    }
}

export default module1
